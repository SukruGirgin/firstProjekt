package Übungen;

public class BinärRechner {

/*
    int binär1;
    int binär2;
    int i = 0;
    int rest = 0;
    int[] summe = new int[20];
    java.util.Scanner scanner = new java.util.Scanner(System.in);

    System.out.print("Input Binär 1: ");
    binär1 = scanner.nextInt();
    System.out.print("Input Binär 2: ");
    binär2 = scanner.nextInt();

    while (binär1 != 0 || binär2 != 0) {
        summe[i++] = (int)((binär1  % 10 + binär2 % 10 + rest) % 2);
        rest = (int)((binär1 % 10 + binär2 % 10 + rest) / 2);
        binär1 = binär1 / 10;
        binär2 = binär2 / 10;
    }

    if (rest != 0) {
        summe[i++] = rest;
    }
    --i;
    System.out.print("Summe beider Binärzahlen: ");
    while (i >= 0) {
        System.out.print(summe[i--]);
    }
    System.out.print(" ");
*/


}
